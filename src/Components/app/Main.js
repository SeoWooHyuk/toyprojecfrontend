import Router from "../router/Router"

function Main() {
	return (
	<main>

        <div className="container">
              <Router></Router>
        </div>
     
    </main>
	);
}

export default Main;